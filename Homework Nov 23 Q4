#include<stdio.h>

int main()
{
float rad,per,area,PI=3.14;
printf("Enter radius in cm : ");
scanf("%g",&rad);
per=2*PI*rad; area =PI*rad*rad;
printf("Perimeter = %g cm\nArea= %g cm sqaure",per,area);

return 0;
}
